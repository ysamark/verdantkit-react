import { Meta, StoryObj } from "@storybook/react";

import { AsideElement } from "./Aside";

const meta: Meta<typeof AsideElement> = {
  title: "components/Aside",
  component: AsideElement,
};

export const Default: StoryObj<typeof meta> = {};

export default meta;
