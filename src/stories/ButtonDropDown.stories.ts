import { Meta, StoryObj } from "@storybook/react";

import { ButtonDropDownElement } from "./ButtonDropDown";

const meta: Meta<typeof ButtonDropDownElement> = {
  title: "components/ButtonDropDown",
  component: ButtonDropDownElement,
  args: {},
};

export const Default: StoryObj<typeof meta> = {};

export default meta;
