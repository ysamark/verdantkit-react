import { Meta, StoryObj } from "@storybook/react";

import { CardButtonsElement } from "./CardButtonsElement";

const meta: Meta<typeof CardButtonsElement> = {
  title: "components/CardButtons",
  component: CardButtonsElement,
};

export const Default: StoryObj<typeof meta> = {};

export default meta;
