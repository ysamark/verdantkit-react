import { Meta, StoryObj } from "@storybook/react";

import { DialogButtonElement } from "./DialogButton";

const meta: Meta<typeof DialogButtonElement> = {
  title: "components/DialogButton",
  component: DialogButtonElement,
};

export const Default: StoryObj<typeof meta> = {};

export default meta;
