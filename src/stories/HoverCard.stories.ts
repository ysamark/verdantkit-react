import { Meta, StoryObj } from "@storybook/react";

import { HoverCardElement } from "./HoverCardElement";

const meta: Meta<typeof HoverCardElement> = {
  title: "components/HoverCard",
  component: HoverCardElement,
  args: {},
};

export const Default: StoryObj<typeof meta> = {};

export default meta;
